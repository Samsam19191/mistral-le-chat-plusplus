// Utility helpers placeholder; add reusable helpers and formatters here.
export function noop(): void {
  // Intentionally empty until utilities are implemented.
}

export function formatGreeting(name: string): string {
  return `Hello, ${name}!`;
}
